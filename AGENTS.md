
- Nunca altere arquivos do projeto. Apenas analise, explique e sugira. Todas as mudanças devem ser feitas manualmente pelo usuário com exceção de quando solicitado ser feito pelo agente.

- Sempre que o usuário apontar que algo não funciona você deve testar (alterando arquivos), validar que a solução é X e então apagar o que você alterou (e unicamente o que alterou, sem limpar worktree) para instruir o usuário.

- Todo código que for gerado deve ser comentado como o restante ta aplicação